# Auto-detect text files and normalise line endings to LF in the repo.
* text=auto eol=lf

# Explicitly handle some binary / well-known types
*.png  binary
*.jar  binary
*.gif  binary
*.ico  binary
*.zip  binary
*.class binary